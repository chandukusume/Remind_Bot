# in credentials.yml

rest:
  # No configuration needed

# Telegram connector disabled - using custom telegram bridge instead
# telegram:
#   access_token: "${TELEGRAM_BOT_TOKEN}"
#   webhook_url: "https://remindbot.chandukusume.me/webhooks/telegram/webhook"